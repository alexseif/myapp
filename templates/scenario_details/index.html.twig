{% extends 'base.html.twig' %}

{% block title %}ScenarioDetails index{% endblock %}

{% block content %}
    <h1>ScenarioDetails index</h1>

    <table class="table">
        <thead>
        <tr>
            <th>Id</th>
            <th>Title</th>
            <th>Date</th>
            <th>Amount</th>
            <th>actions</th>
        </tr>
        </thead>
        <tbody>
        {% for scenario_detail in scenario_details %}
            <tr>
                <td>{{ scenario_detail.id }}</td>
                <td>{{ scenario_detail.title }}</td>
                <td>{{ scenario_detail.date ? scenario_detail.date|date('Y-m-d') : '' }}</td>
                <td>{{ scenario_detail.amount }}</td>
                <td>
                    <a href="{{ path('scenario_details_show', {'scenario': scenario,'id': scenario_detail.id}) }}">show</a>
                    <a href="{{ path('scenario_details_edit', {'scenario': scenario,'id': scenario_detail.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('scenario_details_new', {'scenario': scenario}) }}">Create new</a>
{% endblock %}
