{% extends 'base.html.twig' %}
{% import "macros/a.html.twig" as a %}

{% block panelTitle %}ShoppingList{% endblock %}
{% block panelBack %}{{ a.back('shopping_list_index') }}{% endblock %}
{% block panelHeadingRight %}
    {{ a.edit('shopping_list_edit', {'id':shopping_list.id}) }}
    {{ include('shopping_list/_delete_form.html.twig') }}
{% endblock %}
{% block panelBody %}
    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ shopping_list.id }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ shopping_list.name }}</td>
        </tr>
        </tbody>
    </table>
{% endblock %}
