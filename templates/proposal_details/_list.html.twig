{% import "macros/a.html.twig" as a %}
<table class="table">
    <thead>
    <tr>
        <th>Id</th>
        <th>Title</th>
        <th>Body</th>
        <th>Type</th>
        <th>actions</th>
    </tr>
    </thead>
    <tbody>
    {% for proposal_detail in proposal_details %}
        <tr>
            <td>{{ proposal_detail.id }}</td>
            <td>{{ proposal_detail.title }}</td>
            <td>{{ proposal_detail.body }}</td>
            <td>{{ proposal_detail.type }}</td>
            <td>
                {{ a.show('proposal_details_show', {'proposal':proposal.id, 'id': proposal_detail.id}) }}
                {{ a.edit('proposal_details_edit', {'proposal':proposal.id, 'id': proposal_detail.id}) }}
            </td>
        </tr>
    {% else %}
        <tr>
            <td colspan="5">no records found</td>
        </tr>
    {% endfor %}
    </tbody>
</table>