{% extends 'base.html.twig' %}

{% block title %}ScenarioObjective{% endblock %}

{% block Content %}
    <h1>ScenarioObjective</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ scenario_objective.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ scenario_objective.name }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ scenario_objective.value }}</td>
            </tr>
            <tr>
                <th>Priority</th>
                <td>{{ scenario_objective.priority }}</td>
            </tr>
            <tr>
                <th>Urgency</th>
                <td>{{ scenario_objective.Urgency }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('scenario_objective_index', {'scenario': scenario}) }}">back to list</a>

    <a href="{{ path('scenario_objective_edit', , {'scenario': scenario, 'id': scenario_objective.id}) }}">edit</a>

    {{ include('scenario_objective/_delete_form.html.twig') }}
{% endblock %}
