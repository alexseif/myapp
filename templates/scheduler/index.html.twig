{% extends 'base.html.twig' %}
{% import "macros/forms.html.twig" as forms %}
{% import "macros/a.html.twig" as a %}
{% import "AppBundle:tasks:tasks_macros.html.twig" as tasks_view %}

{% block floatingActions %}{{ a.add('tasks_new') }}{% endblock %}

{% block panelTitle %}Scheduler{% endblock %}

{% block panelBody %}
    <div class="week">
        {% for day, tasks in focus %}
            <div class="day">
                <div class="day-title">{{ day }}</div>
                {#                <ul class="list-group day-body" data-day-lenght="{{ 8 * 60 }}"> #}
                {#                    {% for day in  %} #}
                {{ tasks_view.ul(tasks) }}

                {#                        <li class="list-group-item task" data-est="{{ task.est }}"> #}
                {#                            {{ task.task }} #}
                {#                            <span class="tasklist badge badge-primary">{{ task.taskList.name }}</span> #}
                {#                            <span class="eta badge badge-info">{{ task.est }}</span> #}
                {#                            {{ a.edit('tasks_edit', {'id': task.id}) }} #}
                {#                        </li> #}
                {#                    {% endfor %} #}
                {#                </ul> #}
            </div>
        {% endfor %}
    </div>
{% endblock %}
