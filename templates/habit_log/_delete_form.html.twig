<form method="post" action="{{ path('habit_log_delete', {'id': habit_log.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ habit_log.id) }}">
    <button class="btn">Delete</button>
</form>
