{% extends 'base.html.twig' %}
{% block pageActions %}
    <div class="floating-action">
        <a href="{{ path('accounttransactions_new', { 'account': account.id }) }}" class="bg-primary"><span class="glyphicon glyphicon-plus"></span></a>
    </div>
{% endblock %}
{% block panelBack %}<a href="{{ path('accounts_index') }}" class="btn btn-default btn-sm"><span class="glyphicon glyphicon-arrow-left"></span></a>{% endblock %}

{% block panelTitle %}{{ account.name }}{% endblock %}
{% block panelHeadingRight %}
    {{ form_start(delete_form) }}
    <span class="btn-group btn-group-sm">
        <a href="{{ path('accounts_edit', { 'id': account.id }) }}" class="btn btn-primary btn-secondary">Edit</a>
        <input type="submit" value="Delete" class="btn btn-danger  btn-secondary">
    </span>
    {{ form_end(delete_form) }}
{% endblock %}
{% set accountTransactions = account.transactions %}
{% block panelBody %}
    {% include 'accounttransactions/transactions_list.html.twig' %}
{% endblock %}

{# TODO: show account info#}