{% extends 'base.html.twig' %}

{% block container %}    
        <div class="floating-action">
            <a href="{{ path('days_new') }}" class="bg-primary" role="button"><span class="glyphicon glyphicon-plus"></span></a>
        </div>
    <div class="row">
        <div class="col-sm-4 col-sm-offset-4">
            {% include 'days/days_list.html.twig' %}
        </div>
    </div>
{% endblock %}
